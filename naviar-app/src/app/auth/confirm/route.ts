import { type EmailOtpType } from "@supabase/supabase-js";
import { redirect } from "next/navigation";
import { type NextRequest } from "next/server";
import { createClient } from "@/lib/supabase/server";

/**
 * Email confirmation and magic-link landing route.
 *
 * Uses the token_hash + verifyOtp flow rather than reading a code from the
 * URL fragment, so verification happens server-side and the token never
 * reaches client JavaScript.
 */
export async function GET(request: NextRequest) {
  const { searchParams } = new URL(request.url);
  const token_hash = searchParams.get("token_hash");
  const type = searchParams.get("type") as EmailOtpType | null;
  const next = searchParams.get("next") ?? "/dashboard";

  if (token_hash && type) {
    const supabase = await createClient();
    const { error } = await supabase.auth.verifyOtp({ type, token_hash });
    if (!error) redirect(next.startsWith("/") ? next : "/dashboard");
  }

  redirect("/login?error=link_invalid");
}
