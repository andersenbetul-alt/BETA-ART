# HXI-5 — environment template
# Copy to .env.local and fill in real values

# Site URL (override for local dev)
# NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Email endpoint for stem pack gating (optional)
# STEM_PACK_WEBHOOK=https://...

# No Supabase — this is a pure static site
