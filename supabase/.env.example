# OAuth provider credentials referenced by supabase/config.toml as env(...).
# Copy to supabase/.env and fill in. Never commit the filled-in file.

SUPABASE_AUTH_GOOGLE_CLIENT_ID=
SUPABASE_AUTH_GOOGLE_SECRET=

SUPABASE_AUTH_GITHUB_CLIENT_ID=
SUPABASE_AUTH_GITHUB_SECRET=
