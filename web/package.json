{
  "name": "cobban",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "COBBAN \u2014 Europe is complicated. COBBAN makes it simple.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "check": "npm run typecheck && npm run test && npm run build",
    "verify:apis": "node tests/verify-apis.mjs",
    "smoke": "node tests/smoke.mjs"
  },
  "dependencies": {
    "next": "^16.3.1",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "typescript": "^5.8.3"
  }
}
